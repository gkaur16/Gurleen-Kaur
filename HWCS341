/******************************************************************************

Welcome to GDB Online.
GDB online is an online compiler and debugger tool for C, C++, Python, PHP, Ruby, 
C#, VB, Perl, Swift, Prolog, Javascript, Pascal, HTML, CSS, JS
Code, Compile, Run and Debug online from anywhere in world.

*******************************************************************************/
//
// Homework 1
// CS 341 Spring 2021
// << Gurleen Kaur >>
//

#include <iostream>
#include <fstream>
#include <string>
using namespace std;

int main()
{
	string fileName;
	getline(cin, fileName);

	string line;

	ifstream myfile(fileName);
	
	string subLine;
	string arrayLines[50];
	int i = 0;

	if (myfile.is_open()) {
		while (getline(myfile, line)) {
		    while (line[i] == ' '){
	            i++;
		    }
		    if (line[i-1] == ' '){
		        i--;
		    }
		    
			subLine = line.substr(i, line.size());
		    arrayLines[i] = subLine;
		    i++;
		}
		myfile.close();
	}
	else {
		cout << "Error! Unable to open file";
	}
    
	if (arrayLines[1] == "") {
		cout << arrayLines[0];
	}
	else{
		for (int x = 0; x <= i; x++) {
			if (arrayLines[x + 1] == "") {
				cout << arrayLines[x];
			}
			else {
				cout << arrayLines[x] << endl;
			}
		}
	}

	return 0;
}
